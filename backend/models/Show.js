class Show {
    constructor({ show_id, title, description, created_at }) {
        this.show_id = show_id;
        this.title = title;
        this.description = description;
        this.created_at = created_at;
    }
}

export default Show;
